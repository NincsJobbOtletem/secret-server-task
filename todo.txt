get secret
postal megkapni a rootot

új repo
docker compose docker file src mappa 
1. Symfony fusson

2. utánna adatbázis működjön

3. post get request (postman)
post get route

figyelni a header


csak 3 post legyen amit be viszünk
entity hash ott generálodjuk
created,hash
new date time

factory pattern

ha elfogy a remaining akkor ájon le vagy ha time=exprited


http http2 ✔
application/xml   serializer ✔
factory pattern ✔      SecretFactory a megkapott adatok legyenek ott vmiben

dependency injection

legyen egy metodus a $this json helyett amit megadja a formátumot és a $date-át header alapján.

---------------------------
ures max kezelése

secret kezelése ha 0 akkor örökké

createnél is adatbázissal térjen vissza

factory törlés

google cloud



/////////
google cloud

A docker compose fileba át tenni 80 ra a portot

symfony/(binbash)
symfony check:requirements
composer require symfony/runtime
composer dump-autoload

mysql
symfony console doctrine:migrations:migrate

docker-compose exec database /bin/bash
mysql -u root -p symfony_docker
show tables;
SELECT * FROM secret
